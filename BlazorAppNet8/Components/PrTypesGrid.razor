@namespace BlazorAppNet8
@using System.Net.Http.Json
@inject HttpClient httpClient

<MudDataGrid Items="@prTypes" Filterable="false" SortMode="@SortMode.None" Groupable="false">
    <Columns>
        <PropertyColumn Property="x => x.Id" Title="ID"/>
        <PropertyColumn Property="x => x.Name" Title="Наименование типа"/>
    </Columns>
</MudDataGrid>

@code {
	
	public IEnumerable<PrType> prTypes;
	protected override void OnInitialized()
	{
		PrType prType = new PrType(0, "Все");


	 prTypes = new List<PrType>
	 		{
	 			new PrType(1, "Форма текущей аттестации"),
	 			new PrType(2, "Занятия с обучающимися"),
	 			new PrType(3, "Самостоятельная работа"),
	 			new PrType(4, "Форма промежуточной аттестации"),
	 			new PrType(5, "Основные единицы измерения"),
	 		};
	}
}